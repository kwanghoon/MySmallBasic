list = List.Empty()
'list1 = list

For i = 1 To 10
	List.Add(list, i)
	'1부터 10까지 
	'list = List.Concat(list, List.List(i))
	EndFor

For i = 1 To 10
	TextWindow.WriteLine(List.Head(list))
	List.Tail(list)
EndFor

TextWindow.WriteLine(List.Head(list))